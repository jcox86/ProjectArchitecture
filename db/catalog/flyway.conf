# module: db.catalog
# purpose: Flyway configuration for the catalog (control-plane) database.
# exports:
#   - config: db/catalog/flyway.conf
# patterns:
#   - flyway
# notes:
#   - Secrets (url/user/password) are provided via environment variables or pipeline secrets.

# Locations are relative to the repo root. Run Flyway from the repo root:
#   flyway -configFiles=db/catalog/flyway.conf migrate
flyway.locations=filesystem:db/catalog/migrations,filesystem:db/catalog/repeatable,filesystem:db/catalog/seed

# Keep Flyway state isolated from application schemas.
flyway.defaultSchema=flyway
flyway.schemas=flyway

# Safety defaults.
flyway.cleanDisabled=true
flyway.validateMigrationNaming=true

# Connection (do not commit secrets).
# flyway.url=jdbc:postgresql://<host>:5432/<catalog_db>
# flyway.user=<username>
# flyway.password=<password>

